# Copy to keystore.properties (git-ignored) for local release builds.
# In CI these come from GitHub secrets instead - never commit real values.
KEYSTORE_FILE=release.keystore
KEYSTORE_PASSWORD=
KEY_ALIAS=baatx
KEY_PASSWORD=
BAATX_API_BASE_URL=https://baatx-api-3a1y.onrender.com/
